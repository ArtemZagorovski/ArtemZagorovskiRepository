var x=prompt("x=") 
if(parseInt(x)*0==0)
alert("Число");
else alert("Это не число");

var z=prompt("z=")
if(parseInt(x)*0==0)
alert("Число");
else alert("Это не число");

var a=prompt("a=")
if(parseInt(x)*0==0)
alert("Число");
else alert("Это не число");

var max
max=x;
if(z>max) max=z;
if(a>max) max=a;
alert("max="+max)
